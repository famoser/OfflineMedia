<UserControl
    x:Class="OfflineMediaV3.UserControls.MyDay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OfflineMediaV3.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:OfflineMediaV3.DisplayHelper.Converter"
    xmlns:myDayControls="using:OfflineMediaV3.UserControls.MyDayControls"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400"
    Foreground="White"
    DataContext="{Binding MyDayViewModel, Source={StaticResource Locator}}">

    <StackPanel Background="{ThemeResource AkzentBackgroundThemeBrush}">
        <StackPanel Margin="10,0">
            <Grid Visibility="{Binding Forecast1, Converter={StaticResource ReferenceVisibillityEvaluator}}">
                <myDayControls:Forecast DataContext="{Binding Forecast1}" />
                <Border Style="{ThemeResource WeatherBorder}" />
            </Grid>
            <Grid Visibility="{Binding Forecast2, Converter={StaticResource ReferenceVisibillityEvaluator}}">
                <myDayControls:Forecast DataContext="{Binding Forecast2}" />
                <Border Style="{ThemeResource WeatherBorder}" />
            </Grid>
            <myDayControls:ToDo DataContext="{Binding }" />
        </StackPanel>
    </StackPanel>
</UserControl>
