<UserControl
    x:Class="OfflineMediaV3.UserControls.FeedList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OfflineMediaV3.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{Binding MainPageViewModel.Sources[0], Source={StaticResource Locator}}">

    <Grid>
        <ListView IsItemClickEnabled="True" SelectionMode="None" IsHoldingEnabled="True" ItemsSource="{Binding FeedList}" ItemClick="FeedList_ItemClick">
            <ListView.FooterTemplate>
                <DataTemplate>
                    <TextBlock Height="25" />
                </DataTemplate>
            </ListView.FooterTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:ArticleList DataContext="{Binding }" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</UserControl>
