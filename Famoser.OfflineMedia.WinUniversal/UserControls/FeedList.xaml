<UserControl
    x:Class="Famoser.OfflineMedia.WinUniversal.UserControls.FeedList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:Famoser.OfflineMedia.WinUniversal.UserControls"
    mc:Ignorable="d"
    d:DataContext="{Binding MainPageViewModel.Sources[0], Source={StaticResource Locator}}">

    <Grid>
        <ListView
                  
                  ItemsSource="{Binding ActiveFeeds}" 
                  Style="{StaticResource ListViewStyle}"
                  ItemContainerStyle="{StaticResource ItemContainerStyle}"
                  SelectedItem="{Binding MainPageViewModel.SelectedFeed, Mode=TwoWay, Source={StaticResource Locator}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Background="{ThemeResource FlatGridBackgroundColor}">
                            <TextBlock Text="{Binding Name, Mode=OneWay}" Style="{ThemeResource FamoserHeaderTextBlock}"  />
                        </Grid>
                        <userControls:ArticleList Grid.Row="1" DataContext="{Binding ActiveArticles}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</UserControl>
