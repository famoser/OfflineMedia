<UserControl
    x:Class="OfflineMedia.UserControls.FeedList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:OfflineMedia.UserControls"
    mc:Ignorable="d"
    d:DataContext="{Binding MainPageViewModel.Sources[0], Source={StaticResource Locator}}">

    <Grid>
        <ListView ScrollViewer.VerticalScrollMode="Enabled" IsItemClickEnabled="True" SelectionMode="None" IsHoldingEnabled="True" ItemsSource="{Binding FeedList}" ItemClick="FeedList_ItemClick">
            <ListView.FooterTemplate>
                <DataTemplate>
                    <TextBlock Height="25" />
                </DataTemplate>
            </ListView.FooterTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <userControls:ArticleList DataContext="{Binding }" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>

</UserControl>
